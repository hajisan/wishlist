# Applikationsnavn
spring.application.name=WishList

# Aktiv profil
# dev (development) bruges lokalt under udvikling
spring.profiles.active=dev
# prod (production) bruges n�r vi skal deploy til Azure
# spring.profiles.active=prod

# Databaseforbindelse
spring.datasource.url=jdbc:mysql://localhost:3306/databaseNavnet
spring.datasource.username=brugernavn
spring.datasource.password=adgangskode

# JDBC-driver som Java bruger til at kommunikere med databasen
# com.mysql.cj.jdbc.Driver er JDBC-driveren til MySQL
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver